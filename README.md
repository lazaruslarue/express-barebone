# super simple routed server example

check out what you can do here with a routed server.

experiment with: `window.location`

experiment with `history.pushState` method, try `history.pushState(null, null, '/first')`
